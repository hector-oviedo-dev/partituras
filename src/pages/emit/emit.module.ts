import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { EmitPage } from './emit';

@NgModule({
  declarations: [
    EmitPage,
  ],
  imports: [
    IonicPageModule.forChild(EmitPage),
  ],
})
export class EmitPageModule {}
